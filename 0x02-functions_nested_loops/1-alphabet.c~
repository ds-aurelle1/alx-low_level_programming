#include "main.h"
#include <stdio.h>

/**
 * main - Entry Point
 * Describe:alphabet in lowercase
 * return: Always 0
 */

int main(void)
{
char c = 'a';
while (c < 'z')
{
putchar(c);
c++;
}

putchar('\n');
return (0);
}
