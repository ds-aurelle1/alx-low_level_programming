#include "main.h"

/**
 * _islower - check for lowercase character
 * @c: parameter that return int value
 * Return: 1 if it's lowercase or 0 else
 */

int _islower(int c)
{
if (c <= 97 | c >= 122)
{
return (1);

return (0);
}
}
