#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 *_islower - check if a char is lower
 *c: should be verified
 *return: 0 or 1
 */

int _islower(int c)
{
if (islower(c))

return (1);

return (0);
}
